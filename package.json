{
    "name": "brainfuck",
    "description": "Slightly optimized Brainfuck Interpreter",
    "version": "1.5.1",
    "license": "MIT",
    "scripts": {
        "lint": "eslint --ignore-path .gitignore src/ test/ -f='stylish' --color",
        "format": "prettier --ignore-path .gitignore --write src/ test/",
        "format-check": "prettier --ignore-path .gitignore --check src/ test/",
        "test": "vitest"
    },
    "devDependencies": {
        "@types/node": "^20.2.3",
        "typescript": "^5.0.4",
        "vite-node": "^0.31.1",
        "vitest": "^0.31.1"
    }
}
